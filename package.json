{
  "name": "docquery",
  "version": "1.0.0",
  "description": "AI-powered document analysis system built with Streamlit",
  "main": "app.py",
  "scripts": {
    "build": "vercel build",
    "dev-build": "echo 'Development build completed' && mkdir -p public && echo 'Static files ready'",
    "start": "streamlit run app.py --server.port=$PORT --server.address=0.0.0.0 --server.headless=true",
    "dev": "streamlit run app.py",
    "install": "pip install -r requirements.txt",
    "check-size": "du -sh .vercel/output/functions 2>/dev/null || echo 'No build output found. Run build first.'"
  },
  "repository": {
    "type": "git",
    "url": "https://github.com/TejaswiBhavani/DocQuery"
  },
  "keywords": [
    "ai",
    "document-analysis",
    "streamlit",
    "machine-learning",
    "nlp"
  ],
  "author": "TejaswiBhavani",
  "license": "MIT",
  "engines": {
    "node": ">=14.0.0"
  }
}