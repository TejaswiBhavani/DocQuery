# Essential dependencies for Vercel deployment
# Optimized for serverless functions

# Core FastAPI stack
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
python-multipart>=0.0.6
httpx>=0.25.0
aiofiles>=23.2.0

# Document processing - core only
PyPDF2>=3.0.0
python-docx>=0.8.11

# Scientific computing - minimal versions
numpy>=1.24.0
scikit-learn>=1.3.0

# AI integration
openai>=1.97.1

# Additional utilities for deployment
pydantic>=2.0.0
typing-extensions>=4.0.0

# Note: Streamlit excluded for Vercel deployment (serverless incompatible)
# Note: Heavy ML libraries (sentence-transformers, faiss) excluded for Lambda size limits